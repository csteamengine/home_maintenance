{
  "name": "panel",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --outfile=dist/main.js --format=esm --target=es2020 --minify",
    "build:card": "esbuild src/todo-card.ts --bundle --outfile=dist/todo-card.js --format=esm --target=es2020 --minify",
    "build:all": "npm run build && npm run build:card",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "TJPoorman",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.25.5",
    "lit": "^3.3.0",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "custom-card-helpers": "^1.9.0",
    "@material/mwc-select": "latest",
    "@material/mwc-list": "latest",
    "@mdi/js": "7.4.47",
    "@mdi/svg": "7.4.47"
  }
}